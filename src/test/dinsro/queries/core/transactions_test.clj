(ns dinsro.queries.core.transactions-test)

;; (:require
;;  [clojure.test :refer [deftest use-fixtures]]
;;  [dinsro.mocks :as mocks]
;;  [dinsro.test-helpers :as th]
;;  [fulcro-spec.core :refer [assertions]])

;; [[../../../../main/dinsro/mocks.clj]]
;; [[../../../../main/dinsro/queries/core/transactions.clj]]

;; (def schemata [])

;; (use-fixtures :each (fn [f] (th/start-db f schemata)))

;; (deftest index-by-block
;;   (let [block (mocks/mock-block)]
;;     (assertions
;;      block => nil)))
