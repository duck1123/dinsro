# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

GET     /api/v1/transactions                    Transactions.IndexApi
GET     /api/v1/transactions/:id                Transactions.ShowApi
GET     /api/v1/users                           Users.IndexApi
GET     /api/v1/users/:id                       Users.ShowApi

GET     /                                       App.Index
GET     /*path                                  App.Index
